# Single-GPU trainer defaults (override per run).
accelerator: gpu
devices: 1
precision: bf16-mixed
max_epochs: 50
gradient_clip_val: 1.0
accumulate_grad_batches: 1
log_every_n_steps: 50
deterministic: false
