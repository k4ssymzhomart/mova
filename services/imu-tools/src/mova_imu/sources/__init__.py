"""mova-authored input adapters: capture files and mova `session_frames` rows.

Every adapter emits the same canonical transport event, so the vendored
analysis core does not know or care where its frames came from.
"""
